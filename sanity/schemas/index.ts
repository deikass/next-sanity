import page from './page-schemas';
import project from './project-schema';
import review from './review-schema';
import user from './user-schema';

const schemas = [user, review, project, page];

export default schemas;